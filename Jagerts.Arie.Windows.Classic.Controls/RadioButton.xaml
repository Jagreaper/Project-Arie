<RadioButton x:Class="Jagerts.Arie.Windows.Classic.Controls.RadioButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Jagerts.Arie.Windows.Classic.Controls">
    <RadioButton.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources\ButtonColors.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ArieRadioButtonStyle" TargetType="{x:Type local:RadioButton}">
                <Setter Property="Background" Value="{DynamicResource ArieButtonMainBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ArieButtonMainBorderBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource ArieButtonMainContentBrush}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Padding" Value="10 7"/>
                <Setter Property="BorderThickness" Value="0 4 0 0"/>

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type local:RadioButton}">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource ArieButtonHoverBackgroundBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource ArieButtonHoverBorderBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource ArieButtonHoverContentBrush}"/>
                    </Trigger>

                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{DynamicResource ArieButtonSelectedBackgroundBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource ArieButtonSelectedBorderBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource ArieButtonSelectedContentBrush}"/>
                    </Trigger>

                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource ArieButtonDisabledBackgroundBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource ArieButtonDisabledBorderBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource ArieButtonDisabledContentBrush}"/>
                    </Trigger>
                    
                    <Trigger Property="IsChecked" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource ArieButtonRadioBorderBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="{x:Type local:RadioButton}" BasedOn="{StaticResource ArieRadioButtonStyle}"/>
        </ResourceDictionary>
    </RadioButton.Resources>
</RadioButton>
